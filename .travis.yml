language: python
python:
  - "3.5"
#  - "pypy"
# command to install dependencies
install:
#  - pip install .
  - pip install -r requirements.txt
  - pip install codecov
# command to run tests
script: nosetest --with-coverage
after_success:
  - codecov